apiVersion: v1
kind: Service
metadata:
  name: {{ include "skills-api-chart.fullname" . }}
  labels:
    {{- include "skills-api-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "skills-api-chart.selectorLabels" . | nindent 4 }}
